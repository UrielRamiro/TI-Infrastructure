First things first, install the javascript packages with:

npm i

Inside the backend folder, compile the typescript into java script with:

tsc

To start the infrastructure you should just do a compose up

docker compose up
